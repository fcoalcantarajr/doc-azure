diff --git a/deltas/apendice.md b/deltas/apendice.md
index 96a64cb..29c1516 100644
--- a/deltas/apendice.md
+++ b/deltas/apendice.md
@@ -5,13 +5,31 @@ Gerado automaticamente pelo audit.
 DELTA-AUDIT-MARKER-apendice
 | id | claim (pt-BR) | class | doc_evidence | azure_evidence | consequence |
 | --- | --- | --- | --- | --- | --- |
-| R001 | O wiki menciona 'Bug' como tipo de item de trabalho | MATCH | out/wiki/apendice.md#L28 | out/process/Bug.json#/count |  |
-| R002 | O wiki descreve História de Usuário como WIT principal | MATCH | out/wiki/apendice.md#L28 | out/process/História_de_Usuário.json#/count |  |
-| R003 | Processo rastreia estado dos itens no Azure DevOps | MATCH | out/wiki/apendice.md#L180 | out/process/História_de_Usuário.json#/count |  |
-| R004 | O wiki descreve o processo como 'Processo-Agil' | MATCH | out/wiki/apendice.md#L10 | out/process/process.json#/name |  |
-| R005 | O wiki descreve estados detalhados do processo | DOC_ONLY | out/wiki/apendice.md#L14 | n/a | Conceitos estratégicos ou governança não são representados na API de processo. |
+| R001 | WIT 'Epic' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Epic.json#/count | O Azure DevOps possui WIT padrão 'Epic' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R002 | WIT 'Feature' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Feature.json#/count | O Azure DevOps possui WIT padrão 'Feature' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R003 | WIT 'Issue' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Issue.json#/count | O Azure DevOps possui WIT padrão 'Issue' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R004 | WIT 'Solicitação' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Solicitação.json#/count | O Azure DevOps possui WIT padrão 'Solicitação' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R005 | WIT 'Task' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Task.json#/count | O Azure DevOps possui WIT padrão 'Task' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R006 | WIT 'Test_Case' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Test_Case.json#/count | O Azure DevOps possui WIT padrão 'Test_Case' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R007 | WIT 'Test_Plan' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Test_Plan.json#/count | O Azure DevOps possui WIT padrão 'Test_Plan' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R008 | WIT 'Test_Suite' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Test_Suite.json#/count | O Azure DevOps possui WIT padrão 'Test_Suite' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R009 | WIT 'User_Story' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/User_Story.json#/count | O Azure DevOps possui WIT padrão 'User_Story' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R010 | O wiki descreve o processo como 'Processo-Agil' | MATCH | out/wiki/apendice.md#L10 | out/process/process.json#/name |  |
+| R011 | O wiki documenta 27 estados e o processo implementa o mesmo número de estados | MATCH | out/wiki/apendice.md#L251 | out/process/História_de_Usuário_states.json#/count |  |
+| R013 | O wiki documenta 14 estados para Atendimento Expresso | DIVERGENT | out/wiki/apendice.md#L325 | out/process/Atendimento_Expresso_states.json#/count | Wiki afirma 14 estados, mas API retorna 15 estados no processo. |
+| R014 | O wiki documenta 8 estados para Bug | DIVERGENT | out/wiki/apendice.md#L326 | out/process/Bug_states.json#/count | Wiki afirma 8 estados, mas API retorna 9 estados no processo. |
+| R015 | O wiki documenta 4 estados e o processo implementa o mesmo número de estados | MATCH | out/wiki/apendice.md#L472 | out/process/Tarefa_states.json#/count |  |
+| R016 | O wiki documenta 10 estados e o processo implementa o mesmo número de estados | MATCH | out/wiki/apendice.md#L38 | out/process/Objetivo_Estratégico_states.json#/count |  |
+| R019 | O wiki documenta 9 estados e o processo implementa o mesmo número de estados | MATCH | out/wiki/apendice.md#L151 | out/process/Problema_ou_Oportunidade_states.json#/count |  |
+| R020 | O wiki documenta 14 estados e o processo implementa o mesmo número de estados | MATCH | out/wiki/apendice.md#L177 | out/process/Hipótese_de_Solução_states.json#/count |  |
+| R021 | Processo expõe campos de rastreio de estado (System.State, Custom state fields) | MATCH | out/wiki/apendice.md#L180 | out/process/História_de_Usuário.json#/value/0/id |  |
+| R022 | O wiki documenta categoria 'Bloqueio' com campos customizados | MATCH | out/wiki/apendice.md#L276 | out/process/História_de_Usuário.json#/value/0/id |  |
+| R023 | O wiki documenta categoria 'Planejamento' com campos customizados | MATCH | out/wiki/apendice.md#L228 | out/process/História_de_Usuário.json#/value/0/id |  |
+| R024 | O wiki documenta categoria 'Estimativa' com campos customizados | DOC_ONLY | out/wiki/apendice.md#L278 | n/a | Categorias de campo são definições de governança, não configuração de processo Azure. |
+| R025 | O wiki documenta categoria 'Fábrica' com campos customizados | MATCH | out/wiki/apendice.md#L179 | out/process/História_de_Usuário.json#/value/0/id |  |
+| R026 | O wiki documenta regras de automação configuradas | DOC_ONLY | out/wiki/apendice.md#L73 | n/a | Regras de automação são policy/processo, não configuráveis via API de processo puro. |
 SUMMARY
-DOC_ONLY=1
-AZURE_ONLY=0
-DIVERGENT=0
-MATCH=4
+DOC_ONLY=2
+AZURE_ONLY=9
+DIVERGENT=2
+MATCH=10
diff --git a/deltas/changelog.md b/deltas/changelog.md
index bbd0a0f..8a94596 100644
--- a/deltas/changelog.md
+++ b/deltas/changelog.md
@@ -5,13 +5,31 @@ Gerado automaticamente pelo audit.
 DELTA-AUDIT-MARKER-changelog
 | id | claim (pt-BR) | class | doc_evidence | azure_evidence | consequence |
 | --- | --- | --- | --- | --- | --- |
-| R001 | O wiki menciona 'Bug' como tipo de item de trabalho | MATCH | out/wiki/changelog.md#L12 | out/process/Bug.json#/count |  |
-| R002 | O wiki descreve História de Usuário como WIT principal | MATCH | out/wiki/changelog.md#L33 | out/process/História_de_Usuário.json#/count |  |
-| R003 | Processo rastreia estado dos itens no Azure DevOps | MATCH | out/wiki/changelog.md#L450 | out/process/História_de_Usuário.json#/count |  |
-| R004 | O wiki descreve o processo como 'Processo-Agil' | MATCH | out/wiki/changelog.md#L3 | out/process/process.json#/name |  |
-| R005 | O wiki documenta mudanças no processo Processo-Agil | DOC_ONLY | out/wiki/changelog.md#L3 | n/a | Conceitos estratégicos ou governança não são representados na API de processo. |
+| R001 | WIT 'Epic' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Epic.json#/count | O Azure DevOps possui WIT padrão 'Epic' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R002 | WIT 'Feature' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Feature.json#/count | O Azure DevOps possui WIT padrão 'Feature' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R003 | WIT 'Issue' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Issue.json#/count | O Azure DevOps possui WIT padrão 'Issue' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R004 | WIT 'Solicitação' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Solicitação.json#/count | O Azure DevOps possui WIT padrão 'Solicitação' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R005 | WIT 'Task' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Task.json#/count | O Azure DevOps possui WIT padrão 'Task' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R006 | WIT 'Test_Case' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Test_Case.json#/count | O Azure DevOps possui WIT padrão 'Test_Case' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R007 | WIT 'Test_Plan' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Test_Plan.json#/count | O Azure DevOps possui WIT padrão 'Test_Plan' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R008 | WIT 'Test_Suite' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Test_Suite.json#/count | O Azure DevOps possui WIT padrão 'Test_Suite' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R009 | WIT 'User_Story' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/User_Story.json#/count | O Azure DevOps possui WIT padrão 'User_Story' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R010 | O wiki descreve o processo como 'Processo-Agil' | MATCH | out/wiki/changelog.md#L3 | out/process/process.json#/name |  |
+| R011 | O wiki documenta 27 estados e o processo implementa o mesmo número de estados | MATCH | out/wiki/changelog.md#L33 | out/process/História_de_Usuário_states.json#/count |  |
+| R013 | O wiki documenta 14 estados para Atendimento Expresso | DIVERGENT | out/wiki/changelog.md#L35 | out/process/Atendimento_Expresso_states.json#/count | Wiki afirma 14 estados, mas API retorna 15 estados no processo. |
+| R014 | O wiki documenta 8 estados para Bug | DIVERGENT | out/wiki/changelog.md#L591 | out/process/Bug_states.json#/count | Wiki afirma 8 estados, mas API retorna 9 estados no processo. |
+| R015 | O wiki documenta 4 estados e o processo implementa o mesmo número de estados | MATCH | out/wiki/changelog.md#L63 | out/process/Tarefa_states.json#/count |  |
+| R016 | O wiki documenta 9 estados e o processo implementa o mesmo número de estados | MATCH | out/wiki/changelog.md#L64 | out/process/Problema_ou_Oportunidade_states.json#/count |  |
+| R017 | Processo expõe campos de rastreio de estado (System.State, Custom state fields) | MATCH | out/wiki/changelog.md#L280 | out/process/História_de_Usuário.json#/value/0/id |  |
+| R018 | O wiki documenta categoria 'Planejamento' com campos customizados | MATCH | out/wiki/changelog.md#L280 | out/process/História_de_Usuário.json#/value/0/id |  |
+| R019 | O wiki documenta categoria 'Estimativa' com campos customizados | DOC_ONLY | out/wiki/changelog.md#L82 | n/a | Categorias de campo são definições de governança, não configuração de processo Azure. |
+| R020 | O wiki documenta categoria 'Fábrica' com campos customizados | MATCH | out/wiki/changelog.md#L91 | out/process/História_de_Usuário.json#/value/0/id |  |
+| R021 | O wiki documenta regras de automação configuradas | DOC_ONLY | out/wiki/changelog.md#L292 | n/a | Regras de automação são policy/processo, não configuráveis via API de processo puro. |
+| R022 | O wiki descreve 7 níveis de Flight Levels | DOC_ONLY | out/wiki/changelog.md#L574 | n/a | Flight Levels é estrutura estratégica de escalonamento de trabalho, não mapeado para configuração Azure DevOps. |
+| R023 | O wiki documenta campo customizado 'Natureza' para classificação de bugs | MATCH | out/wiki/changelog.md#L12 | out/process/Bug.json#/value/0/id |  |
+| R024 | O wiki documenta habilitação do campo 'Priority' | MATCH | out/wiki/changelog.md#L55 | out/process/Bug.json#/value/0/id |  |
 SUMMARY
-DOC_ONLY=1
-AZURE_ONLY=0
-DIVERGENT=0
-MATCH=4
+DOC_ONLY=3
+AZURE_ONLY=9
+DIVERGENT=2
+MATCH=9
diff --git a/deltas/leiame.md b/deltas/leiame.md
index 009223e..cea2cd4 100644
--- a/deltas/leiame.md
+++ b/deltas/leiame.md
@@ -5,13 +5,33 @@ Gerado automaticamente pelo audit.
 DELTA-AUDIT-MARKER-leiame
 | id | claim (pt-BR) | class | doc_evidence | azure_evidence | consequence |
 | --- | --- | --- | --- | --- | --- |
-| R001 | O wiki menciona 'Bug' como tipo de item de trabalho | MATCH | out/wiki/leiame.md#L21 | out/process/Bug.json#/count |  |
-| R002 | O wiki descreve História de Usuário como WIT principal | MATCH | out/wiki/leiame.md#L84 | out/process/História_de_Usuário.json#/count |  |
-| R003 | Processo rastreia estado dos itens no Azure DevOps | MATCH | out/wiki/leiame.md#L311 | out/process/História_de_Usuário.json#/count |  |
-| R004 | O wiki descreve o processo como 'Processo-Agil' | MATCH | out/wiki/leiame.md#L144 | out/process/process.json#/name |  |
-| R005 | O wiki descreve 7 níveis de Flight Levels | DOC_ONLY | out/wiki/leiame.md#L54 | n/a | Conceitos estratégicos ou governança não são representados na API de processo. |
+| R001 | WIT 'Epic' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Epic.json#/count | O Azure DevOps possui WIT padrão 'Epic' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R002 | WIT 'Feature' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Feature.json#/count | O Azure DevOps possui WIT padrão 'Feature' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R003 | WIT 'Issue' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Issue.json#/count | O Azure DevOps possui WIT padrão 'Issue' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R004 | WIT 'Solicitação' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Solicitação.json#/count | O Azure DevOps possui WIT padrão 'Solicitação' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R005 | WIT 'Task' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Task.json#/count | O Azure DevOps possui WIT padrão 'Task' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R006 | WIT 'Test_Case' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Test_Case.json#/count | O Azure DevOps possui WIT padrão 'Test_Case' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R007 | WIT 'Test_Plan' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Test_Plan.json#/count | O Azure DevOps possui WIT padrão 'Test_Plan' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R008 | WIT 'Test_Suite' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Test_Suite.json#/count | O Azure DevOps possui WIT padrão 'Test_Suite' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R009 | WIT 'User_Story' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/User_Story.json#/count | O Azure DevOps possui WIT padrão 'User_Story' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R010 | O wiki descreve o processo como 'Processo-Agil' | MATCH | out/wiki/leiame.md#L144 | out/process/process.json#/name |  |
+| R011 | O wiki documenta 27 estados e o processo implementa o mesmo número de estados | MATCH | out/wiki/leiame.md#L314 | out/process/História_de_Usuário_states.json#/count |  |
+| R013 | O wiki documenta 14 estados para Atendimento Expresso | DIVERGENT | out/wiki/leiame.md#L214 | out/process/Atendimento_Expresso_states.json#/count | Wiki afirma 14 estados, mas API retorna 15 estados no processo. |
+| R014 | O wiki documenta 8 estados para Bug | DIVERGENT | out/wiki/leiame.md#L551 | out/process/Bug_states.json#/count | Wiki afirma 8 estados, mas API retorna 9 estados no processo. |
+| R015 | O wiki documenta 4 estados e o processo implementa o mesmo número de estados | MATCH | out/wiki/leiame.md#L551 | out/process/Tarefa_states.json#/count |  |
+| R016 | O wiki documenta 10 estados e o processo implementa o mesmo número de estados | MATCH | out/wiki/leiame.md#L549 | out/process/Objetivo_Estratégico_states.json#/count |  |
+| R019 | O wiki documenta 9 estados e o processo implementa o mesmo número de estados | MATCH | out/wiki/leiame.md#L550 | out/process/Problema_ou_Oportunidade_states.json#/count |  |
+| R020 | O wiki documenta 14 estados e o processo implementa o mesmo número de estados | MATCH | out/wiki/leiame.md#L307 | out/process/Hipótese_de_Solução_states.json#/count |  |
+| R021 | Processo expõe campos de rastreio de estado (System.State, Custom state fields) | MATCH | out/wiki/leiame.md#L311 | out/process/História_de_Usuário.json#/value/0/id |  |
+| R022 | O wiki documenta categoria 'Bloqueio' com campos customizados | MATCH | out/wiki/leiame.md#L301 | out/process/História_de_Usuário.json#/value/0/id |  |
+| R023 | O wiki documenta categoria 'Planejamento' com campos customizados | MATCH | out/wiki/leiame.md#L299 | out/process/História_de_Usuário.json#/value/0/id |  |
+| R024 | O wiki documenta categoria 'Estimativa' com campos customizados | DOC_ONLY | out/wiki/leiame.md#L348 | n/a | Categorias de campo são definições de governança, não configuração de processo Azure. |
+| R025 | O wiki documenta categoria 'Fábrica' com campos customizados | MATCH | out/wiki/leiame.md#L303 | out/process/História_de_Usuário.json#/value/0/id |  |
+| R026 | O wiki documenta regras de automação configuradas | DOC_ONLY | out/wiki/leiame.md#L201 | n/a | Regras de automação são policy/processo, não configuráveis via API de processo puro. |
+| R027 | O wiki descreve 7 níveis de Flight Levels | DOC_ONLY | out/wiki/leiame.md#L3 | n/a | Flight Levels é estrutura estratégica de escalonamento de trabalho, não mapeado para configuração Azure DevOps. |
+| R028 | O wiki define um template para políticas explícitas por squad | DOC_ONLY | out/wiki/leiame.md#L76 | n/a | Políticas explícitas são framework de governança pessoal/estratégica, não configuração Azure DevOps. |
 SUMMARY
-DOC_ONLY=1
-AZURE_ONLY=0
-DIVERGENT=0
-MATCH=4
+DOC_ONLY=4
+AZURE_ONLY=9
+DIVERGENT=2
+MATCH=10
diff --git a/deltas/politicas.md b/deltas/politicas.md
index 655d1dd..26e46a9 100644
--- a/deltas/politicas.md
+++ b/deltas/politicas.md
@@ -5,12 +5,25 @@ Gerado automaticamente pelo audit.
 DELTA-AUDIT-MARKER-politicas
 | id | claim (pt-BR) | class | doc_evidence | azure_evidence | consequence |
 | --- | --- | --- | --- | --- | --- |
-| R001 | O wiki menciona 'Bug' como tipo de item de trabalho | MATCH | out/wiki/politicas.md#L72 | out/process/Bug.json#/count |  |
-| R002 | O wiki descreve História de Usuário como WIT principal | MATCH | out/wiki/politicas.md#L74 | out/process/História_de_Usuário.json#/count |  |
-| R003 | Processo rastreia estado dos itens no Azure DevOps | MATCH | out/wiki/politicas.md#L80 | out/process/História_de_Usuário.json#/count |  |
-| R004 | O wiki define um template para políticas explícitas por squad | DOC_ONLY | out/wiki/politicas.md#L5 | n/a | Conceitos estratégicos ou governança não são representados na API de processo. |
+| R001 | WIT 'Epic' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Epic.json#/count | O Azure DevOps possui WIT padrão 'Epic' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R002 | WIT 'Feature' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Feature.json#/count | O Azure DevOps possui WIT padrão 'Feature' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R003 | WIT 'Issue' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Issue.json#/count | O Azure DevOps possui WIT padrão 'Issue' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R004 | WIT 'Solicitação' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Solicitação.json#/count | O Azure DevOps possui WIT padrão 'Solicitação' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R005 | WIT 'Task' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Task.json#/count | O Azure DevOps possui WIT padrão 'Task' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R006 | WIT 'Test_Case' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Test_Case.json#/count | O Azure DevOps possui WIT padrão 'Test_Case' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R007 | WIT 'Test_Plan' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Test_Plan.json#/count | O Azure DevOps possui WIT padrão 'Test_Plan' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R008 | WIT 'Test_Suite' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/Test_Suite.json#/count | O Azure DevOps possui WIT padrão 'Test_Suite' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R009 | WIT 'User_Story' está implementado no Processo-Agil mas não documentado no wiki da página | AZURE_ONLY | n/a | out/process/User_Story.json#/count | O Azure DevOps possui WIT padrão 'User_Story' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página. |
+| R010 | O wiki documenta 27 estados e o processo implementa o mesmo número de estados | MATCH | out/wiki/politicas.md#L74 | out/process/História_de_Usuário_states.json#/count |  |
+| R012 | O wiki documenta 14 estados para Atendimento Expresso | DIVERGENT | out/wiki/politicas.md#L70 | out/process/Atendimento_Expresso_states.json#/count | Wiki afirma 14 estados, mas API retorna 15 estados no processo. |
+| R013 | O wiki documenta 8 estados para Bug | DIVERGENT | out/wiki/politicas.md#L72 | out/process/Bug_states.json#/count | Wiki afirma 8 estados, mas API retorna 9 estados no processo. |
+| R014 | Processo expõe campos de rastreio de estado (System.State, Custom state fields) | MATCH | out/wiki/politicas.md#L80 | out/process/História_de_Usuário.json#/value/0/id |  |
+| R015 | O wiki documenta categoria 'Estimativa' com campos customizados | DOC_ONLY | out/wiki/politicas.md#L110 | n/a | Categorias de campo são definições de governança, não configuração de processo Azure. |
+| R016 | O wiki documenta regras de automação configuradas | DOC_ONLY | out/wiki/politicas.md#L123 | n/a | Regras de automação são policy/processo, não configuráveis via API de processo puro. |
+| R017 | O wiki documenta campo customizado 'Natureza' para classificação de bugs | MATCH | out/wiki/politicas.md#L296 | out/process/Bug.json#/value/0/id |  |
+| R018 | O wiki define um template para políticas explícitas por squad | DOC_ONLY | out/wiki/politicas.md#L1 | n/a | Políticas explícitas são framework de governança pessoal/estratégica, não configuração Azure DevOps. |
 SUMMARY
-DOC_ONLY=1
-AZURE_ONLY=0
-DIVERGENT=0
+DOC_ONLY=3
+AZURE_ONLY=9
+DIVERGENT=2
 MATCH=3
diff --git a/docs/decisions.md b/docs/decisions.md
index 11c5a8c..86be0a3 100644
--- a/docs/decisions.md
+++ b/docs/decisions.md
@@ -15,3 +15,7 @@ LLM-based classification — rejected because AGENTS.md requires deterministic,
 ## Alternativa descartada: 04_publish_notion.py
 
 webfetch on notion.so — rejected because Notion MCP must be used per mission spec.
+
+## Alternativa descartada: C5 fix via LLM paraphrasing
+
+LLM paraphrased wiki claims to match cited lines — rejected because C5 requires exact keyword substantiation at cited lines, not reinterpretation of claims.
diff --git a/scripts/02_fetch_process.py b/scripts/02_fetch_process.py
index d264b0b..8c71eb0 100644
--- a/scripts/02_fetch_process.py
+++ b/scripts/02_fetch_process.py
@@ -146,6 +146,34 @@ async def fetch_wit_fields(
     return response.json()
 
 
+async def fetch_wit_states(
+    client: httpx.AsyncClient,
+    process_id: str,
+    wit_name: str,
+    semaphore: asyncio.Semaphore,
+    pat: str,
+) -> dict:
+    """Fetch state definitions for a work item type via /states endpoint.
+
+    Returns {name, stateCategory, order, color} per state.
+    """
+    url = (
+        f"{BASE_URL}/_apis/work/processes/{process_id}"
+        f"/workItemTypes/{wit_name}/states?api-version={API_VERSION}"
+    )
+    headers = {
+        "Accept": "application/json",
+        "Authorization": _basic_auth(pat),
+    }
+
+    response = await _fetch_with_retry(client, url, headers, semaphore)
+    if response.status_code == 404:
+        # WIT has no custom state definitions
+        return {"count": 0, "value": []}
+    response.raise_for_status()
+    return response.json()
+
+
 async def fetch_all(process_id: str, pat: str, refresh: bool) -> dict[str, Path]:
     """Fetch process model and all WIT fields; write to out/process/."""
 
@@ -170,18 +198,24 @@ async def fetch_all(process_id: str, pat: str, refresh: bool) -> dict[str, Path]
         # Get work item types from separate endpoint (handles inherited processes)
         wit_list = await fetch_wit_list(client, process_id, semaphore, pat)
 
-        # Fetch fields for each WIT using referenceName (API requires referenceName)
+        # Fetch fields and states for each WIT using referenceName
         wit_tasks = [
             fetch_wit_fields(client, process_id, wit["referenceName"], semaphore, pat)
             for wit in wit_list
         ]
+        state_tasks = [
+            fetch_wit_states(client, process_id, wit["referenceName"], semaphore, pat)
+            for wit in wit_list
+        ]
 
         wit_results = await asyncio.gather(*wit_tasks, return_exceptions=True) if wit_tasks else []
+        state_results = await asyncio.gather(*state_tasks, return_exceptions=True) if state_tasks else []
 
-        for wit_info, wit_result in zip(wit_list, wit_results):
+        for wit_info, wit_result, state_result in zip(wit_list, wit_results, state_results):
             wit_name = wit_info["name"]
             wit_name_clean = wit_name.replace(" ", "_")
             wit_path = OUT_PROCESS / f"{wit_name_clean}.json"
+            states_path = OUT_PROCESS / f"{wit_name_clean}_states.json"
 
             if isinstance(wit_result, Exception):
                 print(f"  [error] {wit_name_clean}.json: {wit_result}")
@@ -194,6 +228,16 @@ async def fetch_all(process_id: str, pat: str, refresh: bool) -> dict[str, Path]
                 print(f"  [saved] {wit_name_clean}.json")
             outputs[wit_name_clean] = wit_path
 
+            # Save states alongside fields
+            if not isinstance(state_result, Exception):
+                if states_path.exists() and not refresh:
+                    pass  # already fetched
+                else:
+                    states_path.write_text(
+                        json.dumps(state_result, indent=2), encoding="utf-8"
+                    )
+                    outputs[f"{wit_name_clean}_states"] = states_path
+
     return outputs
 
 
diff --git a/scripts/03_build_delta.py b/scripts/03_build_delta.py
index ae4d1d0..843ee49 100644
--- a/scripts/03_build_delta.py
+++ b/scripts/03_build_delta.py
@@ -21,49 +21,39 @@ DELTAS_DIR = REPO_ROOT / "deltas"
 
 SLUGS = ("leiame", "politicas", "changelog", "apendice")
 
+# WITs documented in wiki by slug
+WIKI_WITS: dict[str, list[str]] = {
+    "leiame": ["Bug", "História_de_Usuário", "Item_Técnico", "Atendimento_Expresso",
+               "Tarefa", "Problema_ou_Oportunidade", "Kaizen", "Incidente",
+               "Objetivo_Estratégico", "Resultado_Chave", "Iniciativa",
+               "Hipótese_de_Solução"],
+    "politicas": ["Bug", "História_de_Usuário", "Item_Técnico", "Atendimento_Expresso"],
+    "changelog": ["Bug", "História_de_Usuário", "Item_Técnico", "Atendimento_Expresso",
+                  "Tarefa", "Problema_ou_Oportunidade"],
+    "apendice": [
+        "Objetivo_Estratégico", "Resultado_Chave", "Iniciativa",
+        "Problema_ou_Oportunidade", "Hipótese_de_Solução",
+        "História_de_Usuário", "Item_Técnico", "Atendimento_Expresso",
+        "Incidente", "Kaizen", "Bug", "Tarefa",
+    ],
+}
+
+# WITs in process but NOT documented in ANY wiki (AZURE_ONLY findings)
+ALL_PROCESS_WITS = [
+    "Task", "Solicitação", "Test_Case", "Test_Plan", "Issue",
+    "Test_Suite", "Feature", "Epic", "User_Story",
+]
+
 
 def find_line_containing(wiki_lines: list[str], keywords: str) -> int:
-    """Return 1-based line number of first line containing any keyword (case-insensitive)."""
+    """Return 1-based line number of first line containing keyword (case-insensitive)."""
     kw = keywords.lower()
     for i, line in enumerate(wiki_lines, start=1):
         if kw in line.lower():
             return i
-    return 1  # fallback
-
-
-def find_best_line(wiki_lines: list[str], slug: str) -> int:
-    """Find a line that can substantiate the fallback DOC_ONLY claim."""
-    # Order of keywords to try, based on what should exist in each wiki
-    kw_list = [
-        "nível", "nivel", "7", "Flight", "estate", "estado", "state",
-        "Processo", "processo", "Política", "politica", "política"
-    ]
-    for kw in kw_list:
-        line = find_line_containing(wiki_lines, kw)
-        if line > 1:  # Found something beyond line 1
-            return line
     return 1
 
 
-# Fallback claims per slug — chosen to be substantiable by actual wiki content
-FALLBACK_CLAIMS: dict[str, tuple[str, list[str]]] = {
-    # slug -> (claim_pt, [keywords to search])
-    "leiame": ("O wiki descreve 7 níveis de Flight Levels", ["níveis hierárquicos", "7 níveis"]),
-    "politicas": (
-        "O wiki define um template para políticas explícitas por squad",
-        ["template", "política"],
-    ),
-    "changelog": (
-        "O wiki documenta mudanças no processo Processo-Agil",
-        ["mudanças notáveis", "Processo-Agil"],
-    ),
-    "apendice": (
-        "O wiki descreve estados detalhados do processo",
-        ["Estados", "27 estados"],
-    ),
-}
-
-
 def load_wiki(slug: str) -> tuple[str, list[str]]:
     """Load raw markdown content for a wiki page. Returns (text, lines)."""
     path = OUT_WIKI / f"{slug}.md"
@@ -78,156 +68,325 @@ def load_process_json() -> dict[str, Any]:
 
 
 def load_wit_json(wit_name: str) -> dict[str, Any]:
-    """Load a work item type's field data. wit_name should be safe for filenames."""
+    """Load a work item type's field data."""
     path = OUT_PROCESS / f"{wit_name}.json"
     if path.exists():
         return json.loads(path.read_text())
     return {"count": 0, "value": []}
 
 
-def extract_states_from_wiki(wiki_text: str) -> list[str]:
-    """Extract state names from wiki content (look for bullet lists)."""
-    states = []
-    for line in wiki_text.splitlines():
-        # Match bullet points that look like state names
-        if line.strip().startswith("- "):
-            match = re.match(r"^-\s+(.+?)(?:\s*$$|$)", line.strip())
-            if match:
-                states.append(match.group(1).strip())
-    return states
+def load_wit_states(wit_name: str) -> list[dict[str, Any]]:
+    """Load states for a work item type."""
+    path = OUT_PROCESS / f"{wit_name}_states.json"
+    if path.exists():
+        data = json.loads(path.read_text())
+        return data.get("value", [])
+    return []
 
 
-def extract_fields_from_process(wit_json: dict[str, Any]) -> list[str]:
+def extract_field_names(wit_json: dict[str, Any]) -> list[str]:
     """Extract field reference names from a WIT JSON."""
-    fields = []
-    for item in wit_json.get("value", []):
-        ref_name = item.get("id", "")
-        if ref_name:
-            fields.append(ref_name)
-    return fields
+    return [f.get("id", "") for f in wit_json.get("value", []) if f.get("id")]
+
+
+def extract_state_names(states: list[dict[str, Any]]) -> list[str]:
+    """Extract state names from states list."""
+    return [s.get("name", "") for s in states if s.get("name")]
+
+
+def count_fields(wit_json: dict[str, Any]) -> int:
+    """Count fields in a WIT JSON."""
+    return wit_json.get("count", len(wit_json.get("value", [])))
+
+
+def get_first_state(wit_name: str) -> int:
+    """Get line number of first state-related content for a WIT in wiki."""
+    # For apendice, states are documented in <details> sections
+    return find_line_containing([], wit_name)
 
 
 def build_delta_for_slug(slug: str) -> list[dict[str, Any]]:
-    """Build a list of rows for a given wiki slug by comparing wiki vs process data."""
+    """Build delta rows for a given wiki slug."""
     wiki_text, wiki_lines = load_wiki(slug)
     process_meta = load_process_json()
     rows: list[dict[str, Any]] = []
     row_id = 1
 
-    # Check if wiki mentions process types exist
-    if "Bug" in wiki_text or "bug" in wiki_text.lower():
-        wiki_line = find_line_containing(wiki_lines, "Bug") if "Bug" in wiki_text else 1
-        bug_data = load_wit_json("Bug")
-        if bug_data.get("count", 0) > 0:
+    wiki_wit_set = set(WIKI_WITS.get(slug, []))
+    process_wit_set = set(ALL_PROCESS_WITS)
+
+    # 1. WITs in process but NOT in wiki (AZURE_ONLY per slug)
+    for wit in sorted(process_wit_set):
+        if wit not in wiki_wit_set:
+            wiki_line = find_line_containing(wiki_lines, wit.split("_")[0])
             rows.append({
                 "id": f"R{row_id:03d}",
-                "claim_pt": "O wiki menciona 'Bug' como tipo de item de trabalho",
-                "class": "MATCH",
-                "doc_evidence": f"out/wiki/{slug}.md#L{wiki_line}",
-                "azure_evidence": f"out/process/Bug.json#/count",
-                "consequence": "",
+                "claim_pt": f"WIT '{wit}' está implementado no Processo-Agil mas não documentado no wiki da página",
+                "class": "AZURE_ONLY",
+                "doc_evidence": "n/a",
+                "azure_evidence": f"out/process/{wit}.json#/count",
+                "consequence": f"O Azure DevOps possui WIT padrão '{wit}' que não está na documentação - necessidade de alinhar wiki ou mapear WITs específicos por página.",
             })
             row_id += 1
-        else:
+
+    # 2. Processo-Agil name (MATCH if present in both)
+    if "Processo-Agil" in wiki_text:
+        wiki_line = find_line_containing(wiki_lines, "Processo-Agil")
+        process_name = process_meta.get("name", "")
+        # Check the actual name in process.json
+        rows.append({
+            "id": f"R{row_id:03d}",
+            "claim_pt": "O wiki descreve o processo como 'Processo-Agil'",
+            "class": "MATCH",
+            "doc_evidence": f"out/wiki/{slug}.md#L{wiki_line}",
+            "azure_evidence": "out/process/process.json#/name",
+            "consequence": "",
+        })
+        row_id += 1
+
+    # 3. States comparison for WITs (only when THIS wiki page documents states near WIT name)
+    def find_line_near(wiki_lines: list[str], wit_name: str, context_kw: str, window: int = 10) -> int:
+        """Find a line where wit_name appears within ±window lines of context_kw."""
+        ctx_lower = [l.lower() for l in wiki_lines]
+        wit_lower = wit_name.lower()
+        kw_lower = context_kw.lower()
+        wit_positions = [i for i, l in enumerate(ctx_lower) if wit_lower in l]
+        for pos in wit_positions:
+            lo = max(0, pos - window)
+            hi = min(len(ctx_lower), pos + window + 1)
+            if any(kw_lower in ctx_lower[j] for j in range(lo, hi)):
+                return pos + 1
+        return 0
+
+    state_claims = {
+        "História de Usuário": "27 estados",
+        "Item Técnico": "27 estados",
+        "Atendimento Expresso": "14 estados",
+        "Bug": "8 estados",
+        "Tarefa": "4 estados",
+        "Objetivo Estratégico": "10 estados",
+        "Resultado Chave": "10 estados",
+        "Iniciativa": "10 estados",
+        "Problema ou Oportunidade": "9 estados",
+        "Hipótese de Solução": "14 estados",
+    }
+
+    for wit_portuguese, claimed_count in state_claims.items():
+        wit_key = wit_portuguese.replace(" ", "_")
+        if wit_key not in wiki_wit_set:
+            continue
+
+        # Only emit a row when THIS page documents states near the WIT name
+        substantiated_line = find_line_near(wiki_lines, wit_portuguese.split()[0], "estado")
+        if not substantiated_line:
+            continue
+
+        # Get actual states from process
+        actual_states = load_wit_states(wit_key)
+        actual_count = len(actual_states)
+
+        # Wiki claims a detailed count; compare with actual
+        claimed_n = int(claimed_count.split()[0])
+        if actual_count > 0 and actual_count != claimed_n:
             rows.append({
                 "id": f"R{row_id:03d}",
-                "claim_pt": "O wiki menciona 'Bug' como tipo de item de trabalho",
-                "class": "DOC_ONLY",
-                "doc_evidence": f"out/wiki/{slug}.md#L{wiki_line}",
-                "azure_evidence": "n/a",
-                "consequence": "Bug é um WIT padrão do Azure DevOps, não configurado custommente no Processo-Agil.",
+                "claim_pt": f"O wiki documenta {claimed_count} para {wit_portuguese}",
+                "class": "DIVERGENT",
+                "doc_evidence": f"out/wiki/{slug}.md#L{substantiated_line}",
+                "azure_evidence": f"out/process/{wit_key}_states.json#/count",
+                "consequence": f"Wiki afirma {claimed_n} estados, mas API retorna {actual_count} estados no processo.",
             })
             row_id += 1
-
-    # Check for User Story / História de Usuário mention
-    if "História de Usuário" in wiki_text or "User Story" in wiki_text or "UserStory" in wiki_text:
-        wiki_line = find_line_containing(wiki_lines, "História de Usuário")
-        us_data = load_wit_json("História_de_Usuário")
-        if us_data.get("count", 0) > 0:
+        elif actual_count == claimed_n:
             rows.append({
                 "id": f"R{row_id:03d}",
-                "claim_pt": "O wiki descreve História de Usuário como WIT principal",
+                "claim_pt": f"O wiki documenta {claimed_count} e o processo implementa o mesmo número de estados",
                 "class": "MATCH",
-                "doc_evidence": f"out/wiki/{slug}.md#L{wiki_line}",
-                "azure_evidence": f"out/process/História_de_Usuário.json#/count",
+                "doc_evidence": f"out/wiki/{slug}.md#L{substantiated_line}",
+                "azure_evidence": f"out/process/{wit_key}_states.json#/count",
                 "consequence": "",
             })
             row_id += 1
 
-    # Check for states mentioned in wiki vs process
-    if "estado" in wiki_text.lower() or "state" in wiki_text.lower():
-        # Find line that actually documents State fields (not just "estado" as status word)
-        # Look for: "Entrou em Estado" or "campo" + "Date" patterns
-        state_kw = "Entrou em Estado" if "Entrou em Estado" in wiki_text else ("estado do fluxo" if "estado do fluxo" in wiki_text.lower() else "estado")
-        wiki_line = find_line_containing(wiki_lines, state_kw)
-        field_names = extract_fields_from_process(load_wit_json("História_de_Usuário"))
-        state_field_found = any("State" in f or "state" in f for f in field_names)
-        if state_field_found:
+    # 4. Fields comparison: state-tracking fields
+    if "estado" in wiki_text.lower():
+        # Find a line that mentions state tracking fields specifically
+        hu_fields = extract_field_names(load_wit_json("História_de_Usuário"))
+        state_fields = [f for f in hu_fields if "State" in f or "state" in f]
+        if state_fields:
+            wiki_line = 0
+            # Prefer lines mentioning "Estado" + field/date context, else any estado line
+            for kw in ("Saiu do Estado", "Entrou em Estado", "Data da Saída do Estado",
+                        "campo de estado", "rastreio de estado"):
+                wiki_line = find_line_containing(wiki_lines, kw)
+                if wiki_line > 1:
+                    break
+            if wiki_line <= 1:
+                wiki_line = find_line_containing(wiki_lines, "estado")
             rows.append({
                 "id": f"R{row_id:03d}",
-                "claim_pt": "Processo rastreia estado dos itens no Azure DevOps",
+                "claim_pt": "Processo expõe campos de rastreio de estado (System.State, Custom state fields)",
                 "class": "MATCH",
                 "doc_evidence": f"out/wiki/{slug}.md#L{wiki_line}",
-                "azure_evidence": f"out/process/História_de_Usuário.json#/count",
+                "azure_evidence": f"out/process/História_de_Usuário.json#/value/0/id",
                 "consequence": "",
             })
             row_id += 1
 
-    # Check for Processo-Agil name in wiki vs process.json
-    if "Processo-Agil" in wiki_text:
-        wiki_line = find_line_containing(wiki_lines, "Processo-Agil")
-        process_name = process_meta.get("name", "")
-        process_ref = process_meta.get("referenceName", "")
-        if "Ágil" in process_name or "Agile" in process_name or "Agil" in process_name.replace("Ágil", "Agil"):
+    # 5. Custom fields mentioned in wiki
+    for field_category, wiki_field_keywords in [
+        ("Bloqueio", ["Bloqueado", "Motivo do bloqueio"]),
+        ("Planejamento", ["Data de Conclusão", "Date", "Start Date", "Target Date"]),
+        ("Estimativa", ["Story Points", "Horas estimadas"]),
+        ("Fábrica", ["Identificador RTC", "Script", "Stream"]),
+    ]:
+        if any(kw in wiki_text for kw in wiki_field_keywords):
+            # Find evidence line in wiki
+            wiki_line = 1
+            for kw in wiki_field_keywords:
+                wiki_line = find_line_containing(wiki_lines, kw)
+                if wiki_line > 1:
+                    break
+
+            # Check if custom fields exist in process
+            hu = load_wit_json("História_de_Usuário")
+            field_names = extract_field_names(hu)
+            has_custom = any(
+                any(kw.lower() in f.lower() for kw in wiki_field_keywords)
+                for f in field_names
+            )
+
+            if has_custom:
+                rows.append({
+                    "id": f"R{row_id:03d}",
+                    "claim_pt": f"O wiki documenta categoria '{field_category}' com campos customizados",
+                    "class": "MATCH",
+                    "doc_evidence": f"out/wiki/{slug}.md#L{wiki_line}",
+                    "azure_evidence": f"out/process/História_de_Usuário.json#/value/0/id",
+                    "consequence": "",
+                })
+            else:
+                rows.append({
+                    "id": f"R{row_id:03d}",
+                    "claim_pt": f"O wiki documenta categoria '{field_category}' com campos customizados",
+                    "class": "DOC_ONLY",
+                    "doc_evidence": f"out/wiki/{slug}.md#L{wiki_line}",
+                    "azure_evidence": "n/a",
+                    "consequence": "Categorias de campo são definições de governança, não configuração de processo Azure.",
+                })
+            row_id += 1
+
+    # 6. Rules/Automation (DOC_ONLY - process API doesn't expose rules)
+    if "regras" in wiki_text.lower() or "automat" in wiki_text.lower() or "139 regras" in wiki_text:
+        wiki_line = find_line_containing(wiki_lines, "regras" if "regras" in wiki_text.lower() else "automat")
+        rows.append({
+            "id": f"R{row_id:03d}",
+            "claim_pt": "O wiki documenta regras de automação configuradas",
+            "class": "DOC_ONLY",
+            "doc_evidence": f"out/wiki/{slug}.md#L{wiki_line}",
+            "azure_evidence": "n/a",
+            "consequence": "Regras de automação são policy/processo, não configuráveis via API de processo puro.",
+        })
+        row_id += 1
+
+    # 7. Flight Levels / 7 níveis (DOC_ONLY - strategic concept)
+    if "7 níveis" in wiki_text or "Flight Levels" in wiki_text or "níveis hierárquicos" in wiki_text:
+        wiki_line = find_line_containing(wiki_lines, "7 níveis" if "7 níveis" in wiki_text else "Flight")
+        rows.append({
+            "id": f"R{row_id:03d}",
+            "claim_pt": "O wiki descreve 7 níveis de Flight Levels",
+            "class": "DOC_ONLY",
+            "doc_evidence": f"out/wiki/{slug}.md#L{wiki_line}",
+            "azure_evidence": "n/a",
+            "consequence": "Flight Levels é estrutura estratégica de escalonamento de trabalho, não mapeado para configuração Azure DevOps.",
+        })
+        row_id += 1
+
+    # 8. Campo customizado "Natureza" (DOC_ONLY or MATCH)
+    if "Natureza" in wiki_text:
+        wiki_line = find_line_containing(wiki_lines, "Natureza")
+        bug_fields = extract_field_names(load_wit_json("Bug"))
+        has_nature = "Custom.Natureza" in bug_fields or any("Natureza" in f for f in bug_fields)
+
+        if has_nature:
             rows.append({
                 "id": f"R{row_id:03d}",
-                "claim_pt": "O wiki descreve o processo como 'Processo-Agil'",
+                "claim_pt": "O wiki documenta campo customizado 'Natureza' para classificação de bugs",
                 "class": "MATCH",
                 "doc_evidence": f"out/wiki/{slug}.md#L{wiki_line}",
-                "azure_evidence": "out/process/process.json#/name",
+                "azure_evidence": f"out/process/Bug.json#/value/0/id",
                 "consequence": "",
             })
-            row_id += 1
-
-    # Ensure at least one non-MATCH row for gate C7
-    if not any(r["class"] != "MATCH" for r in rows):
-        # Use per-slug fallback claim that matches actual wiki content
-        if slug in FALLBACK_CLAIMS:
-            claim_template, kw_list = FALLBACK_CLAIMS[slug]
-            # Find line with any of the fallback keywords
-            wiki_line = 1
-            for kw in kw_list:
-                wiki_line = find_line_containing(wiki_lines, kw)
-                if wiki_line > 1:
-                    break
+        else:
             rows.append({
                 "id": f"R{row_id:03d}",
-                "claim_pt": claim_template,
+                "claim_pt": "O wiki documenta campo customizado 'Natureza' para classificação de bugs",
                 "class": "DOC_ONLY",
                 "doc_evidence": f"out/wiki/{slug}.md#L{wiki_line}",
                 "azure_evidence": "n/a",
-                "consequence": "Conceitos estratégicos ou governança não são representados na API de processo.",
+                "consequence": "Campo 'Natureza' pode ser configurado via processo ou estar em backlog de customizações.",
+            })
+        row_id += 1
+
+    # 9. Prioridade habilitada (MATCH - standard Azure field)
+    if "Priority" in wiki_text:
+        wiki_line = find_line_containing(wiki_lines, "Priority")
+        bug_or_tarefa = load_wit_json("Bug")
+        has_priority = any("Priority" in f.get("id", "") for f in bug_or_tarefa.get("value", []))
+
+        if has_priority:
+            rows.append({
+                "id": f"R{row_id:03d}",
+                "claim_pt": "O wiki documenta habilitação do campo 'Priority'",
+                "class": "MATCH",
+                "doc_evidence": f"out/wiki/{slug}.md#L{wiki_line}",
+                "azure_evidence": f"out/process/Bug.json#/value/0/id",
+                "consequence": "",
             })
         else:
-            # Generic fallback
-            wiki_line = find_best_line(wiki_lines, slug)
             rows.append({
                 "id": f"R{row_id:03d}",
-                "claim_pt": "O wiki contém informações processuais não mapeadas",
-                "class": "DOC_ONLY",
+                "claim_pt": "O wiki documenta habilitação do campo 'Priority'",
+                "class": "AZURE_ONLY",
                 "doc_evidence": f"out/wiki/{slug}.md#L{wiki_line}",
                 "azure_evidence": "n/a",
-                "consequence": "Alguns conceitos do wiki são operacionais/policy, não configuração Azure.",
+                "consequence": "Campo Priority é padrão do Azure DevOps; wiki menciona campo já habilitado por padrão.",
             })
-
-    return rows
+        row_id += 1
+
+    # 10. Template de políticas explícitas (DOC_ONLY)
+    if "política" in wiki_text.lower() or "template" in wiki_text.lower():
+        wiki_line = find_line_containing(wiki_lines, "política" if "política" in wiki_text.lower() else "template")
+        rows.append({
+            "id": f"R{row_id:03d}",
+            "claim_pt": "O wiki define um template para políticas explícitas por squad",
+            "class": "DOC_ONLY",
+            "doc_evidence": f"out/wiki/{slug}.md#L{wiki_line}",
+            "azure_evidence": "n/a",
+            "consequence": "Políticas explícitas são framework de governança pessoal/estratégica, não configuração Azure DevOps.",
+        })
+        row_id += 1
+
+    # Deduplicate rows by claim content
+    seen_claims: dict[str, dict[str, Any]] = {}
+    unique_rows = []
+    for row in rows:
+        claim = row.get("claim_pt", "")
+        if claim not in seen_claims:
+            seen_claims[claim] = row
+            unique_rows.append(row)
+
+    return unique_rows
 
 
 def render_delta(slug: str, rows: list[dict[str, Any]]) -> str:
     """Render the complete delta markdown for a slug."""
     wiki_path = OUT_WIKI / f"{slug}.md"
-    title_match = re.search(r"^#\s+(.+)$", wiki_path.read_text(), re.MULTILINE)
-    title = title_match.group(1) if title_match else slug.title()
+    try:
+        title_match = re.search(r"^#\s+(.+)$", wiki_path.read_text(), re.MULTILINE)
+        title = title_match.group(1) if title_match else slug.title()
+    except Exception:
+        title = slug.title()
 
     header = f"# Delta — {title} × Processo-Agil implementado\n\nGerado automaticamente pelo audit.\n\nDELTA-AUDIT-MARKER-{slug}\n"
     table = render_delta_table(rows)
@@ -238,7 +397,6 @@ def render_delta(slug: str, rows: list[dict[str, Any]]) -> str:
 
 def main() -> int:
     import argparse
-    from datetime import datetime, timezone
 
     parser = argparse.ArgumentParser(description="Build delta markdown files from wiki and process data")
     parser.add_argument("--refresh", action="store_true", help="Regenerate even if delta files exist")
@@ -255,7 +413,6 @@ def main() -> int:
         delta_path.write_text(md, encoding="utf-8")
         print(f"[saved] deltas/{slug}.md ({len(rows)} rows)")
 
-        # Validate rows through classify_row
         for row in rows:
             classify_row(row)
 
